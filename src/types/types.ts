export type MacroCardProps = {
    label: string,
    value: string,
    goal: string,
    color: string
}

export type MealItemProps = {
    name: string;
    calories: string;
    protein: string;
    carbs: number;
    fat: number;
};