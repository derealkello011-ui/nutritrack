export type MacroCardProps = {
    label: string,
    value: string,
    goal: string,
    color: string
}

export type MealItemProps = {
    name: string;
    calories: number;
    protein: number;
    carbs: number;
    fat: number;
};